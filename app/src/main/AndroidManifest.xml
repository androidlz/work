<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.seventeenok.test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".UI.scrollAnimator.ScrollAnimatorActivity" />
        <activity android:name=".UI.recyclerview.RecyclerViewActivity" />
        <activity android:name=".UI.appbar_layout.AppBarActivity" />
        <activity android:name=".UI.collapsingToolbarLayout.CollapsingActivity" />
        <activity android:name=".UI.custom.CustomActivity">

        </activity>

        <service
            android:name=".MyService"
            android:enabled="true"
            android:exported="true"
            android:process=":remote"
            tools:ignore="ExportedService" />

        <activity android:name=".UI.MainTestActivity">

        </activity>
        <activity android:name=".UI.anim.AnimActivity" />
        <activity android:name=".android.TestActivity" />
        <activity android:name=".UI.md_anim.MdAnimActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>